# Base URL of the backend API.
# Leave unset for local development (Vite proxies /api to http://127.0.0.1:3001)
# and when the backend serves the built frontend (same origin).
# VITE_API_BASE_URL=http://127.0.0.1:3001
